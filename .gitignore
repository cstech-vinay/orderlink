.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
.env
.env.local
.env.*.local
node_modules/

# === Next.js ===
.next/
out/
next-env.d.ts
*.tsbuildinfo

# === Node ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === Test ===
coverage/
playwright-report/
test-results/

# === Generated invoices (local runs) ===
data/invoices/

# === Secrets (service accounts, signing keys, certs — NEVER commit) ===
secrets/
*.serviceaccount.json
sf-jwt.pem

# === Image-gen pipeline: raw Gemini PNG outputs (keep only optimized .webp in git) ===
public/assets/products/*/generated/
assets/products/*/generated/

# === Salesforce JWT private key — NEVER commit. Public cert (.crt) is fine to keep. ===
certs/sf-jwt.key
certs/*.pem
certs/*.p12
certs/*.pfx

